node_modules
dist
dist-ssr
*.local
.DS_Store
# Compiled translation catalogs are generated by `neokapi-i18n compile`.
public/translations
# kapi project state (block store, TM, caches) — all regenerable.
# NOTE: the committed review state lives in .kapi-state.json (tracked), not here.
.kapi/
